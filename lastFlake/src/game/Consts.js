export default class Consts {

    static debug = true;
    static showWalls = false;

    static player = {
        startX: 350,
        startY: 1500, //1000 //1260 //1500,
        speed: 800
    };

    static worldSize = {
        width: 3000,
        height: 1600
    };

    static viewSize = {
        width: 1000,
        height: 800
    };
}