export default class Config {

    static Debug = {
        Global: true,
        Log: true,
        TraceLog: true,
    };

    // static BoardSize = 4;
    // static BoardSize = 6;
    static BoardSize = 8;
}